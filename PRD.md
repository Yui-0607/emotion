# 见微 · 人生观察者 — 产品需求文档

## 1. 产品定位
一个本地运行的个人情绪追踪 + AI 自我观察工具。用户每日记录心情，AI 顺着时间线轻声说出情绪的变化，帮用户看见自己没注意到的模式。

## 2. 技术方案
- 单文件 HTML，数据存 localStorage
- AI 调用 DeepSeek API（deepseek-chat 模型）
- 可选 PowerShell 本地服务器（也可直接双击 HTML 使用）

## 3. 功能模块

### 3.1 每日记录
- 状态评分：1-10 滑块，两端标签「沉郁 ↔ 澄澈」
- 情绪标签：快乐 / 焦虑 / 孤独 / 满足 / 迷茫 / 愤怒，三列网格单选
- 自定义情绪：20 种 emoji 选择器 + 自由文本
- 日记正文：多行输入，上限 2000 字

### 3.2 记忆追问
- **触发时机**：保存日记后自动执行
- **数据范围**：最近 5 条记录
- **数据格式**：按时间正序排列，标注相对日期（今天 / 昨天 / 前天 / 三天前…）
- **AI 行为**：从前往后读，感受情绪如何一天天流动，说出变化的过程
- **口吻**：不提问、不说教、不用问号，像朋友轻声的观察
- **语言**：朴实直白，不用修饰词和比喻，简单有力量
- **容错**：网络失败自动重试 3 次（间隔 2s / 4s）

### 3.3 手动观察
- **触发时机**：用户点击「做一次观察」
- **数据范围**：全部历史记录（≥5 条才可触发）
- **AI 行为**：生成 2-3 段深度观察，关注反复出现的事、情绪转折、被忽视的成长
- **口吻与语言**：同 3.2

### 3.4 定期自动回望
- **触发时机**：每次打开页面自动检查

| 类型 | 时间跨度 | 最少记录 | 
|------|---------|---------|
| 本周回望 | ≥7 天 | 3 条 |
| 月度回望 | ≥30 天 | 7 条 |
| 半年回望 | ≥180 天 | 15 条 |
| 年度回望 | ≥365 天 | 30 条 |

- 每条期限只生成一次，有新记录自动刷新

### 3.5 隐藏功能
- 每条记录可隐藏 / 显示
- 隐藏后卡片半透明保留
- 隐藏的记录不被发送给 AI（追问、回望、观察均自动过滤）

## 4. 数据流
```
用户保存日记 → localStorage 存储
              → 触发 askFU → 取最近 5 条 → 调 DeepSeek → 显示观察

用户打开页面 → checkPeriodic → 判断期限 → 调 DeepSeek → 显示回望

用户点击观察 → genObs → 取全部记录 → 调 DeepSeek → 显示深度观察
```

## 5. 已修复的 Bug
| 问题 | 根因 | 修复 |
|------|------|------|
| Emoji 乱码 | `.split("")` 拆散 surrogate pairs | 改用 `[...]` 展开运算符 |
| 追问崩溃 | 重试函数缺 `async` 关键字 | 补充 |
| start.bat 假死 | 脚本阻塞等待服务器 | 后台启动 + 延迟开浏览器 |
| API Key 未转发 | 代理服务器空代码块 | 正确读写 Auth header |